{
  "name": "zor",
  "version": "0.5.0",
  "private": true,
  "scripts": {
    "build": "bun run --filter zor-code build",
    "dev": "bun run --watch packages/zor-code/src/main.tsx",
    "test": "bun run --filter zor-code test",
    "check": "bun run --filter zor-code typecheck && bun run --filter zor-code test"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "^4.1.9",
    "typescript": "^5.4.0",
    "vitest": "^4.1.9"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}